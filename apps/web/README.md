# Catholic Commons UI Documentation App

This app is the documentation and component gallery for Catholic Commons UI.

Run from the repository root:

```bash
pnpm install
pnpm --filter @ccui/web dev
```

Review gate:

```bash
pnpm lint
pnpm typecheck
pnpm build
pnpm audit
```
